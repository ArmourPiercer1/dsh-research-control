# SUBAGENT_ROUTING.md — 子代理执行路由策略

> 状态：**Active**（用户指令，2026-08-22 记录）
> 适用范围：本项目中拉起任何子代理执行任务时（`subagent` / `subagent_fork` / workflow `agent()` / ralph worker 等一切委派通道）。
> 本文件是执行策略的持久记录：任何会话中拉起子代理前应先读本文件并按策略路由。

## 路由策略（三级，严格顺序）

1. **默认路由**：`qiyuan-self/qwen3.8-27b`；失败允许**重试一次**（同一任务重新执行）；
2. **升级路由**：重试仍失败 → 升级至 `qiyuan-inter/glm-5.3` 执行；
3. **终态**：`glm-5.3` 执行仍失败 → **暂停工作，留待用户裁决**。不得自行更换其他模型、不得继续降级重试、不得绕过暂停。

## 审查代理路由（用户指令，2026-08-22 补充）

审查代理是**显式指定模型**的委派，不适用上述三级默认阶梯；按门禁档位路由：

| 门禁档位 | 审查代理构成 | 模型 |
|---|---|---|
| 权威审查 | 1 名审查代理 | `qiyuan-inter/glm-5.3` |
| 对抗式审查 | 3 名独立审查代理（三角色面板，见执行编排计划 §5.3） | `qiyuan-inter/glm-5.3` 与 `qwen3.8-max` **共同执行** |

对抗式面板的固定分配（约束：两模型必须均参与，保证模型多样性以降低相关性盲区）：

| 角色 | 模型 | 分配理由 |
|---|---|---|
| 规范符合性猎手 | `qiyuan-inter/glm-5.3` | 逐条对照冻结契约，重精确遵循 |
| 不变量攻击者 | `qwen3.8-max` | 攻击视角交给不同模型，最大化视角多样性 |
| 集成与宿主专家 | `qiyuan-inter/glm-5.3` | DSH 一手验证多为 glm-5.3 会话积累 |

审查代理失败处理：**同模型重试一次**；仍失败 → **暂停待用户裁决**（显式指定的模型不得替换——替换会破坏面板构成与用户指定的执行者，故不走默认阶梯的升级路径）。

> 注：`qwen3.8-max` 按用户原始表述记录（未带 provider 前缀）；首次调用时以实际可解析的形式执行，并在 STATUS.md 路由事件中记录实际标识符。

## 执行要点

- 「失败」界定：子代理运行失败 / 超时 / 未产出约定结果。重试前允许**收窄任务范围**以提升成功率（如拆分任务、给出更精确的写作指令），但**路由模型不得变更**；
- workflow 工具的 `agent(prompt, opts)` 支持 `provider`/`model` 独立覆盖，`meta.phases` 亦可按阶段声明——并行编排时按第 1 级声明默认路由，失败项按策略升级；`pipeline`/`parallel` 中抛错降为 `null` 的项视为失败，纳入升级判定；
- 单次 `subagent`/`subagent_fork` 委派若无法指定 provider/model，则在结果报告中**明确注明实际使用的模型**及是否偏离本策略；
- 每次升级（第 2 级触发）与每次暂停（第 3 级触发）都必须在会话中向用户显式报告，不得静默切换；
- 本策略的变更仅由用户指令驱动；变更时更新本文件并在计划书 §40 附录补记。
